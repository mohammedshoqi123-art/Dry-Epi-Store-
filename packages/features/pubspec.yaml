name: dry_features
description: Feature modules for Dry EPI Store
version: 1.0.0
publish_to: none

environment:
  sdk: '>=3.6.0 <4.0.0'
  flutter: '>=3.27.0'

dependencies:
  flutter:
    sdk: flutter
  dry_core:
    path: ../core
  dry_shared:
    path: ../shared
  flutter_riverpod: ^2.5.1
  flutter_form_builder: ^9.2.1
  go_router: ^14.1.4
  supabase_flutter: ^2.5.6
  fl_chart: ^0.68.0
  intl: ^0.19.0
  csv: ^6.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0
